/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn.sanity.io"],
      },
    // cdn.sanity.io
};

export default nextConfig;

//Note yaha humnay images k tag use kia hy or jis web address say images utha rahe hy osko yaha define kiya hy.ab ishe website say nextjs images uthayga.